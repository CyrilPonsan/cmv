{
  "name": "cmv",
  "version": "1.0.0",
  "description": "système d'information pour la clinique fictive montvert - projet de formation",
  "homepage": "https://github.com/CyrilPonsan/cmv#readme",
  "bugs": {
    "url": "https://github.com/CyrilPonsan/cmv/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CyrilPonsan/cmv.git"
  },
  "license": "ISC",
  "author": "cyril.ponsan@gmail.com",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "set-venv": "cd cmv_gateway/cmv_back && python3.13 -m venv venv && source venv/bin/activate && cd ../../cmv_patients && python3.13 -m venv venv && source venv/bin/activate && cd ../cmv_chambres && python3.13 -m venv venv && source venv/bin/activate",
    "set-env": "python3.13 create_env.py",
    "install-gateway": "npm i && cd cmv_gateway/cmv_back && source venv/bin/activate && pip install -r requirements.txt && cd ../cmv_front && npm install",
    "install-patients": "cd cmv_patients && source venv/bin/activate && pip install -r requirements.txt",
    "install-chambres": "cd cmv_chambres && source venv/bin/activate && pip install -r requirements.txt",
    "install:app": "npm i && npm run install-gateway && npm run install-patients && npm run install-chambres",
    "start:preprod": "docker network create cmv || true&& docker compose -f preprod-docker-compose.yml up --build",
    "start-gateway": "npm run start --prefix cmv_gateway",
    "start-patients": "cd cmv_patients && source venv/bin/activate && uvicorn app.main:app --reload --port 8002",
    "start-chambres": "cd cmv_chambres && source venv/bin/activate && uvicorn app.main:app --reload --port 8003",
    "start:dev": "docker network create cmv || true && docker compose -f dev-docker-compose.yml up -d && concurrently \"npm run start-gateway\" \"npm run start-patients\" \"npm run start-chambres\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
