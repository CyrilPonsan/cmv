services:
  redis:
    image: valkey/valkey:latest
    ports:
      - "6379:6379"

  test:
    build:
      context: .
    depends_on:
      - redis
    env_file:
      - .env.test
    command: pytest -s app/tests/

    develop:
      watch:
        - path: .
          action: rebuild
